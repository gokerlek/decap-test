{
  "name": "agency-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd site && pnpm dev",
    "prebuild": "node scripts/img-build.mjs || true",
    "build": "cd site && pnpm build",
    "export": "cd site && pnpm export",
    "preview": "cd site && pnpm preview"
  },
  "dependencies": {
    "globby": "^14",
    "sharp": "^0.33"
  }
}
